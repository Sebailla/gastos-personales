// Default `app/` tree for Next.js 16 (App Router).
// Empty placeholder — Slice A scaffolds the floor; the first
// real page lands in Slice B (T-023 signIn page).
export default function HomePage() {
  return (
    <main>
      <h1>gastos-personales</h1>
      <p>Auth foundation ready. The application surface ships in Slice B.</p>
    </main>
  );
}
