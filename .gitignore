node_modules
.next
dist
coverage
coverage.*
out
*.log
pnpm-debug.log*
.env
.env.local
.env.production
.env.development
!.env.example
.pnpm-store
.DS_Store
prisma/*.db
prisma/*.db-journal
tsconfig.tsbuildinfo
.atl/.skill-registry.cache.json
.atl/skill-registry.md

# Local agent / skill runtime state (not part of the project)
.atl/
.pi/
.arl/
.opencode/
.tmp/
.worktrees/

# Local intermediate output (SDD report artifacts, mirror-pdf-drive work, etc.)
file-only/
dist/mirror-pdf-drive/

# 4R review outputs (subagent artifacts; not deliverables)
review-*.md
progress.md

# Local pnpm runtime artifact (some pnpm versions create a stray
# .npmrc on install that breaks the pre-commit hook — see
# AGENTS.md §9.7). The repo does not need a .npmrc — pnpm reads
# pnpm-workspace.yaml if any, otherwise its built-in defaults.
.npmrc
